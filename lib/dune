(menhir
  (flags --table --dump --strict --explain)
  (modules parser))

(library
 (inline_tests)
 (name unnamed_programming_lang)
 (libraries base sedlex menhirLib)
 (preprocess
  (pps ppx_inline_test sedlex.ppx)))
